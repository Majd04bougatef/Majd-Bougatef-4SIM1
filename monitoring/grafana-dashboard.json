{
  "annotations": {"list": []},
  "panels": [
    {
      "type": "graph",
      "title": "JVM Heap Used",
      "targets": [
        {"expr": "jvm_memory_used_bytes{area=\"heap\"}", "legendFormat": "heap used"}
      ],
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8}
    },
    {
      "type": "graph",
      "title": "HTTP Requests (count)",
      "targets": [
        {"expr": "sum(rate(http_server_requests_seconds_count[1m])) by (uri)", "legendFormat": "{{uri}}"}
      ],
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8}
    }
  ],
  "schemaVersion": 16,
  "title": "Spring Boot Actuator - Basic",
  "version": 1
}
